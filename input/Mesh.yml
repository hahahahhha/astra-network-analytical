# Network Configuration

# 3D basic-topology, Ring_FullyConnected_Switch
topology: [ Mesh ]  # Ring, Switch, FullyConnected, Mesh

# Ring with 16 NPUs
npus_count: [ 16,8]  # rows , cols

# Bandwidth per each dimension
bandwidth: [ 50.0 ]  # GB/s

# Latency per each dimension
latency: [ 500.0 ]  # ns
